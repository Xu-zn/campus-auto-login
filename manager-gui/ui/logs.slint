// 日志查看页面 — Slint 兼容版
import { Button, ScrollView, TextEdit } from "std-widgets.slint";

export component LogsPage inherits Rectangle {
    width: 100%;
    height: 100%;
    background: #0d0d1a;

    in-out property <string> log-content: "";
    in-out property <int>    log-line-count: 0;
    in-out property <int>    log-total-count: 0;
    in-out property <string> log-filter-level: "all";

    callback refresh-logs();
    callback clear-logs();
    callback export-logs();

    VerticalLayout {
        padding: 20px;
        spacing: 12px;

        // 工具栏
        Rectangle {
            height: 36px;
            HorizontalLayout {
                spacing: 8px;
                Text { text: "日志查看"; font-size: 18px; font-weight: 600; color: #ebebf5; }

                Rectangle {
                    background: #1a1a30;
                    border-radius: 8px;
                    HorizontalLayout {
                        padding: 2px;
                        spacing: 2px;

                        Rectangle { background: root.log-filter-level == "all" ? #6c63ff30 : #00000000; border-radius: 6px;
                            HorizontalLayout {
                                padding-left: 10px; padding-right: 10px;
                                padding-top: 5px; padding-bottom: 5px;
                                Text { text: "全部"; font-size: 11px; font-weight: 500; color: root.log-filter-level == "all" ? #a78bfa : #9898b0; }
                            }
                            TouchArea { clicked => { root.log-filter-level = "all"; } }
                        }
                        Rectangle { background: root.log-filter-level == "info" ? #60a5fa30 : #00000000; border-radius: 6px;
                            HorizontalLayout {
                                padding-left: 10px; padding-right: 10px;
                                padding-top: 5px; padding-bottom: 5px;
                                Text { text: "INFO"; font-size: 11px; font-weight: 500; color: root.log-filter-level == "info" ? #60a5fa : #9898b0; }
                            }
                            TouchArea { clicked => { root.log-filter-level = "info"; } }
                        }
                        Rectangle { background: root.log-filter-level == "warn" ? #f4a26130 : #00000000; border-radius: 6px;
                            HorizontalLayout {
                                padding-left: 10px; padding-right: 10px;
                                padding-top: 5px; padding-bottom: 5px;
                                Text { text: "WARN"; font-size: 11px; font-weight: 500; color: root.log-filter-level == "warn" ? #f4a261 : #9898b0; }
                            }
                            TouchArea { clicked => { root.log-filter-level = "warn"; } }
                        }
                        Rectangle { background: root.log-filter-level == "error" ? #ff658430 : #00000000; border-radius: 6px;
                            HorizontalLayout {
                                padding-left: 10px; padding-right: 10px;
                                padding-top: 5px; padding-bottom: 5px;
                                Text { text: "ERROR"; font-size: 11px; font-weight: 500; color: root.log-filter-level == "error" ? #ff6584 : #9898b0; }
                            }
                            TouchArea { clicked => { root.log-filter-level = "error"; } }
                        }
                    }
                }

                Button { text: "↻ 刷新"; clicked => { root.refresh-logs(); } }
                Button { text: "清除"; clicked => { root.clear-logs(); } }
            }
        }

        // 日志内容区
        Rectangle {
            background: #141428;
            border-radius: 12px;
            border-width: 1px;
            border-color: #ffffff10;
            VerticalLayout {
                Rectangle {
                    height: 32px;
                    background: #1a1a30;
                    HorizontalLayout {
                        padding: 8px;
                        Text {
                            text: "显示 " + root.log-line-count + " 条 · 共 " + root.log-total-count + " 条";
                            font-size: 11px; color: #6b6b80;
                            font-family: "Cascadia Code";
                        }
                    }
                }

                ScrollView {
                    TextEdit {
                        text <=> root.log-content;
                        font-size: 11px;
                        font-family: "Cascadia Code";
                        wrap: word-wrap;
                        read-only: true;
                        min-height: 360px;
                    }
                }

                Rectangle {
                    height: 36px;
                    background: #1a1a30;
                    HorizontalLayout {
                        padding: 8px;
                        spacing: 8px;
                        Text { text: "自动刷新间隔: 3s"; font-size: 11px; color: #6b6b80; vertical-alignment: center; }
                        Button { text: "⬇ 导出日志"; clicked => { root.export-logs(); } }
                    }
                }
            }
        }
    }
}
