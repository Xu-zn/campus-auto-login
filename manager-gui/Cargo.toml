[package]
name = "manager-gui"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
slint = "1.12"
campus-core = { path = "../campus-core" }

[build-dependencies]
embed-resource = {workspace = true}
slint-build = "1.12"